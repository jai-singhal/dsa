#include<iostream>
#include<vector>
#include<deque>
#include<queue>

using namespace std;

vector<int> slidingMaximum(const vector<int> &A, int B) {
    0
}

int main(){
    vector<int> A = {1, 3, -1, -3, 5, 3, 6, 7};
    int B = 3;
    vector<int> sol = slidingMaximum(A, B);
    for(auto b: sol){
        cout << b << " ";
    }
    cout << endl;
}